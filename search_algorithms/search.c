#include <stdio.h>

int main()
{
    /*static array - an array that never changes*/
    //int numbers[] = {20, 500, 1, 5, 100, 1, 50};
    int n;
    printf("Number: ");
    scanf("%d", &n);
    printf("the num: %d", n);

    return 0;

}